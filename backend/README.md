# Backend

Run the API locally:

```bash
uvicorn app.main:app --reload --port 8000
```

Endpoints:
- /health
- /api/auth/login
- /api/chat/
- /api/memory/
- /api/graph/
- /api/evaluation/
- /api/tools/
